# OpenPalm — Local Development Environment
#
# 1. Run the setup helper:  ./dev/dev-setup.sh
#    (creates .env with absolute paths and seeds .dev/ config directories)
#
# 2. Start the stack:
#    docker compose --project-directory . --env-file .env -f packages/lib/src/embedded/state/docker-compose.yml -f dev/docker-compose.dev.yml up -d --build
#
# If you prefer to set up manually, copy this file to .env and replace
# the OPENPALM_*_HOME values with absolute paths on your machine.

# ── XDG-style directory layout (use absolute paths) ─────────────────
OPENPALM_DATA_HOME=/REPLACE/WITH/ABSOLUTE/PATH/.dev/data
OPENPALM_CONFIG_HOME=/REPLACE/WITH/ABSOLUTE/PATH/.dev/config
OPENPALM_STATE_HOME=/REPLACE/WITH/ABSOLUTE/PATH/.dev/state

# ── Container runtime ───────────────────────────────────────────────
OPENPALM_CONTAINER_PLATFORM=docker
OPENPALM_COMPOSE_BIN=docker
OPENPALM_COMPOSE_SUBCOMMAND=compose
OPENPALM_CONTAINER_SOCKET_PATH=/var/run/docker.sock
OPENPALM_CONTAINER_SOCKET_IN_CONTAINER=/var/run/openpalm-container.sock
OPENPALM_CONTAINER_SOCKET_URI=unix:///var/run/openpalm-container.sock
OPENPALM_IMAGE_NAMESPACE=openpalm
OPENPALM_IMAGE_TAG=latest

# ── Secrets (replace these for anything beyond throwaway dev) ───────
ADMIN_TOKEN=dev-admin-token
POSTGRES_PASSWORD=dev-pg-password
CHANNEL_CHAT_SECRET=dev-chat-secret
CHANNEL_DISCORD_SECRET=dev-discord-secret
CHANNEL_VOICE_SECRET=dev-voice-secret
CHANNEL_TELEGRAM_SECRET=dev-telegram-secret
CHANNEL_API_SECRET=dev-api-secret

# ── Network / bind address ─────────────────────────────────────────
# OPENPALM_INGRESS_BIND_ADDRESS=0.0.0.0
OPENPALM_UID=1000
OPENPALM_GID=1000

# ── Container-internal mount paths (usually don't need changing) ───
# OPENPALM_DATA_ROOT=/data
# OPENPALM_CONFIG_ROOT=/config
# OPENPALM_STATE_ROOT=/state

# ── OpenMemory Dashboard ─────────────────────────────────────────────
# OPENMEMORY_DASHBOARD_API_URL=http://localhost:8765
# OPENMEMORY_USER_ID=default-user
# OPENPALM_OPENMEMORY_DASHBOARD_BIND_ADDRESS=0.0.0.0
