# OpenPalm — System Configuration Schema (managed by CLI/admin)
# Do not edit manually. These values are written by the CLI and admin.
#
# @defaultSensitive=false
# @defaultRequired=true
# ---

# ── Authentication ────────────────────────────────────────────────────

# Admin API authentication token.
# @type=string(minLength=8) @required @sensitive
OPENPALM_ADMIN_TOKEN=

# Assistant-only operational token.
# @type=string(minLength=32) @required @sensitive
ASSISTANT_TOKEN=

# ── Paths ─────────────────────────────────────────────────────────────

# @type=string(startsWith=/)
OPENPALM_HOME=

# @type=integer(min=0, max=65534)
OPENPALM_UID=1000

# @type=integer(min=0, max=65534)
OPENPALM_GID=1000

# @type=string
OPENPALM_DOCKER_SOCK=/var/run/docker.sock

# ── Images ────────────────────────────────────────────────────────────

# @type=string @sensitive=false
OPENPALM_IMAGE_NAMESPACE=openpalm

# @type=string @sensitive=false
OPENPALM_IMAGE_TAG=latest

# ── Ports (38XX range) ────────────────────────────────────────────────

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_ASSISTANT_PORT=3800

# @type=integer(min=1, max=65535) @sensitive=false @required=false
OPENPALM_CHANNEL_VOICE_PORT=3810

# @type=integer(min=1, max=65535) @sensitive=false @required=false
OPENPALM_CHANNEL_CHAT_PORT=3820

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_ADMIN_PORT=3880

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_ADMIN_OPENCODE_PORT=3881

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_SCHEDULER_PORT=3897

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_MEMORY_PORT=3898

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_GUARDIAN_PORT=3899

# @type=integer(min=1, max=65535) @sensitive=false
OPENPALM_INGRESS_PORT=3080

# ── Networking ────────────────────────────────────────────────────────
# SECURITY: changing any bind address from 127.0.0.1 to 0.0.0.0 exposes
# the corresponding service publicly. Only change with explicit intent.

# @type=string @sensitive=false
OPENPALM_INGRESS_BIND_ADDRESS=127.0.0.1

# @type=string @sensitive=false @required=false
OPENPALM_MEMORY_BIND_ADDRESS=127.0.0.1

# @type=string @sensitive=false @required=false
OPENPALM_ASSISTANT_BIND_ADDRESS=127.0.0.1

# @type=string @sensitive=false @required=false
OPENPALM_ASSISTANT_SSH_BIND_ADDRESS=127.0.0.1

# @type=integer(min=1,max=65535) @sensitive=false @required=false
OPENPALM_ASSISTANT_SSH_PORT=2222

# ── Service Auth ──────────────────────────────────────────────────────

# Auth token for memory service API. Auto-generated on first install.
# @type=string(minLength=32) @required @sensitive
MEMORY_AUTH_TOKEN=

# OpenCode server password for assistant container.
# @type=string(minLength=32) @required=false @sensitive
OPENCODE_SERVER_PASSWORD=

# ── Components ────────────────────────────────────────────────────────

# @type=enum(true,false) @sensitive=false @required=false
OPENPALM_OLLAMA_ENABLED=false

# @type=enum(true,false) @sensitive=false @required=false
OPENPALM_ADMIN_ENABLED=false

# ── Setup Status ──────────────────────────────────────────────────────

# @type=enum(true,false) @sensitive=false @required=false
OPENPALM_SETUP_COMPLETE=false

# ── Channel HMAC Secrets ──────────────────────────────────────────────
# Dynamic keys: CHANNEL_<n>_SECRET are generated per installed channel.
# @type=string(minLength=32) @sensitive
# CHANNEL_*_SECRET=
