# OpenPalm — User Secrets
#
# Place this file at CONFIG_HOME/secrets.env (default: ~/.config/openpalm/secrets.env).
#
# Only user-provided secrets belong here. All infrastructure configuration
# (paths, UID/GID, networking, image tags, OpenMemory URLs) is auto-detected
# by the admin and written to STATE_HOME/artifacts/stack.env. You never need
# to set those values manually.

# ── Required ─────────────────────────────────────────────────────────
# Admin API authentication token — change this before exposing the stack
ADMIN_TOKEN=

# ── LLM Provider Keys (at least one recommended) ────────────────────
# Required for OpenMemory; optional for the assistant
OPENAI_API_KEY=
OPENAI_BASE_URL=
# GROQ_API_KEY=
# MISTRAL_API_KEY=
# GOOGLE_API_KEY=

# ── OpenMemory ───────────────────────────────────────────────────────
OPENMEMORY_USER_ID=default_user

# ── Optional: non-default XDG directories ───────────────────────────
# Uncomment and set if you want custom install paths.
# Docker compose inherits these from the shell env automatically.
# OPENPALM_CONFIG_HOME=/custom/path
# OPENPALM_DATA_HOME=/custom/path
# OPENPALM_STATE_HOME=/custom/path
# OPENPALM_WORK_DIR=/custom/path
