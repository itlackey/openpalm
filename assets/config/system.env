# OpenPalm generates a working .env from this system template during install.
# This file is system-managed and should only be modified manually by experienced users.
# For user-specific overrides, prefer assets/config/user.env (copied to $OPENPALM_CONFIG_HOME/user.env at install time).
#
#   Data   (databases, vectors, blobs)  → XDG_DATA_HOME  or ~/.local/share
#   Config (agent configs, Caddyfile)   → XDG_CONFIG_HOME or ~/.config
#   State  (runtime state, logs, work)  → XDG_STATE_HOME  or ~/.local/state

OPENPALM_DATA_HOME=
OPENPALM_CONFIG_HOME=
OPENPALM_STATE_HOME=

# Container runtime/compose configuration (set by installer scripts)
OPENPALM_CONTAINER_PLATFORM=docker
OPENPALM_COMPOSE_BIN=docker
OPENPALM_COMPOSE_SUBCOMMAND=compose
OPENPALM_CONTAINER_SOCKET_PATH=/var/run/docker.sock
OPENPALM_CONTAINER_SOCKET_IN_CONTAINER=/var/run/docker.sock
OPENPALM_CONTAINER_SOCKET_URI=unix:///var/run/docker.sock
OPENPALM_IMAGE_NAMESPACE=openpalm
OPENPALM_IMAGE_TAG=latest

# Required generated secrets (created automatically during install)
ADMIN_TOKEN=replace-with-long-random-token
POSTGRES_PASSWORD=replace-with-pg-password
CHANNEL_CHAT_SECRET=replace-with-channel-chat-secret
CHANNEL_DISCORD_SECRET=replace-with-channel-discord-secret
CHANNEL_VOICE_SECRET=replace-with-channel-voice-secret
CHANNEL_TELEGRAM_SECRET=replace-with-channel-telegram-secret

# Optional overrides/examples
# POSTGRES_DB=openpalm
# POSTGRES_USER=openpalm
# TOOL_NETWORK_ALLOWLIST=example.com,api.open-meteo.com
# CHAT_INBOUND_TOKEN=
# OPENCODE_TIMEOUT_MS=15000
# OPENPALM_INGRESS_BIND_ADDRESS=0.0.0.0
# OPENPALM_OPENMEMORY_BIND_ADDRESS=0.0.0.0
# OPENCODE_CORE_BIND_ADDRESS=127.0.0.1
# OPENCODE_ENABLE_SSH=0
# OPENCODE_CORE_SSH_BIND_ADDRESS=127.0.0.1
# OPENCODE_CORE_SSH_PORT=2222
# OPENMEMORY_URL=http://openmemory:8765
# OPENMEMORY_POSTGRES_URL=
# OPENMEMORY_QDRANT_URL=

# OpenMemory Dashboard
# OPENMEMORY_DASHBOARD_API_URL=http://localhost:8765
# OPENMEMORY_USER_ID=default-user
# OPENPALM_OPENMEMORY_UI_BIND_ADDRESS=0.0.0.0

# DISCORD_BOT_TOKEN=
# DISCORD_PUBLIC_KEY=
# TELEGRAM_WEBHOOK_SECRET=
# TELEGRAM_BOT_TOKEN=
