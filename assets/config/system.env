# OpenPalm — system configuration template
#
# This file is the initial template for .env. The installer copies it to .env
# and fills in generated values (secrets, paths, runtime detection).
#
# Do not edit this file directly in a production install unless you know
# what you are doing. User-configurable values belong in config/secrets.env
# and config/stack-spec.json.
#
# For local development, see dev/.env.example.

# ── Image configuration ──────────────────────────────────────────────────────
# Override to switch image registry namespace.
OPENPALM_IMAGE_NAMESPACE=openpalm

# ── XDG-style directory layout ───────────────────────────────────────────────
# Set by the installer to absolute paths on your machine.
OPENPALM_DATA_HOME=
OPENPALM_CONFIG_HOME=
OPENPALM_STATE_HOME=

# ── Container runtime ────────────────────────────────────────────────────────
# Detected and set by the installer.
OPENPALM_CONTAINER_PLATFORM=
OPENPALM_COMPOSE_BIN=
OPENPALM_COMPOSE_SUBCOMMAND=
OPENPALM_CONTAINER_SOCKET_PATH=
OPENPALM_CONTAINER_SOCKET_IN_CONTAINER=/var/run/openpalm-container.sock
OPENPALM_CONTAINER_SOCKET_URI=
OPENPALM_IMAGE_TAG=

# ── Secrets ──────────────────────────────────────────────────────────────────
# Generated by the installer with cryptographically-secure random values.
ADMIN_TOKEN=
POSTGRES_PASSWORD=
CHANNEL_CHAT_SECRET=
CHANNEL_DISCORD_SECRET=
CHANNEL_VOICE_SECRET=
CHANNEL_TELEGRAM_SECRET=

# ── Feature flags ────────────────────────────────────────────────────────────
# Populated by the admin at setup time.
OPENPALM_ENABLED_CHANNELS=
