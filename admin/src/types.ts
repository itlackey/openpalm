// Re-export all types from @openpalm/lib for backwards compatibility
export type {
  Approval,
  MessageRequest,
  ChannelMessage,
  MemoryRecord,
  AuditEvent,
  ProviderConnection,
  ConnectionType,
  Connection,
  ModelAssignment,
  AccessScope,
} from "@openpalm/lib";
