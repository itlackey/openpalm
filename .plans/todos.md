- [x] update sveltekit and all npm packages
- [x] improve admin code organization and quality
    - [x] move logic to core/admin/src/lib
    - [x] create well structured components
    - [x] no inline styles, proper CSS structure and scoping
    - [x] modern css features, such as nesting
    - [x] no visual regressions
- [x] implement shared logger in lib and migrate all console.log calls to the logger
- [x] move admin, assistant, and guardian into a ./core directory
- [x] add convenience scripts to ./package.json for local dev workflows
- [x] add connections screen to admin
 - [x] allows users to configure opencode providers that are shared between guardian and assistant
    - see @opencode-providers.md for a reference article
 - [x] allows user to configure LLM connection/opencode provider and model for the guardian to use
 - [x] allows user to configure the openai url, key, and models to use for openmemory
- [x] add a check in the admin ui to direct the user to the connections page after login if there are missing connection settings
- [x] add/clarify buttons in the admin ui for:
    - [x] applying changes to the stack
    - [x] updating all containers (pulling latest docker image)
    - [x] stop, start, restart buttons for individual containers
