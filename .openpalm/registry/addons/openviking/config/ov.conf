{
  "storage": {
    "workspace": "/workspace",
    "vectordb": {
      "dimension": ${OV_EMBEDDING_DIMS},
      "distance_metric": "cosine"
    }
  },
  "embedding": {
    "dense": {
      "provider": "${OV_EMBEDDING_PROVIDER}",
      "model": "${OV_EMBEDDING_MODEL}",
      "api_key": "${OV_EMBEDDING_API_KEY}",
      "api_base": "${OV_EMBEDDING_BASE_URL}",
      "dimension": ${OV_EMBEDDING_DIMS}
    }
  },
  "server": {
    "host": "0.0.0.0",
    "port": 1933,
    "root_api_key": "${OPENVIKING_API_KEY}"
  },
  "auto_generate_l0": true,
  "auto_generate_l1": true
}
