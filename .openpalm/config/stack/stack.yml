# OpenPalm stack specification (v2).
#
# This seed file is overwritten by the setup wizard with your actual
# configuration. It exists so that install-time seeding always has a
# valid v2 reference. See docs/technical/core-principles.md for the
# authoritative format.
version: 2
