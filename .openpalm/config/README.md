User managed configuration directory
- assistant: the assistant's global opencode directory
- automations: directory used by the scheduler
- guardian: the guardian's global opencode directory
- host.yaml - info collected about the host during install
- stack.yaml - the main configuration file used by the @openpalm/lib for managing the stack

