User managed secret store

- the system may set values under the stack directory
- the user can modify values under the stack directory as needed, but it's risky
- the user directory is mounted by the assistant and is used to share secrets with the assistant at runtime
