name: Retired extension packages

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Retired. Guardian and the unified portal are private image components; MCP is the public integration contract."
