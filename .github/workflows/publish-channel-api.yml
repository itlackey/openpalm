name: Publish @openpalm/channel-api
on:
  push:
    branches: [main]
    paths: ['packages/channel-api/**']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 1.2.0, minor, prerelease). Leave empty for auto patch bump.'
        required: false
        type: string
jobs:
  publish:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package-dir: packages/channel-api
      package-name: '@openpalm/channel-api'
      version: ${{ inputs.version || '' }}
