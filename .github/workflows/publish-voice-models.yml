name: Retired voice model bundle

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Retired. Voice is not part of the lean OpenPalm stack."
