name: Publish @openpalm/memory
on:
  push:
    branches: [main]
    paths: ['packages/memory/**']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 1.2.0, minor, prerelease). Leave empty for auto patch bump.'
        required: false
        type: string
permissions:
  contents: write
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package-dir: packages/memory
      package-name: '@openpalm/memory'
      version: ${{ inputs.version || '' }}
