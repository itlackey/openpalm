name: Retired assistant model bundle

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice::Retired. The lean Assistant image no longer embeds a separate model or embedding bundle."
