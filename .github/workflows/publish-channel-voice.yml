name: Publish @openpalm/channel-voice
on:
  push:
    branches: [main]
    paths: ['packages/channel-voice/**']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 1.2.0, minor, prerelease). Leave empty for auto patch bump.'
        required: false
        type: string
permissions:
  contents: write
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/publish-npm-package.yml
    with:
      package-dir: packages/channel-voice
      package-name: '@openpalm/channel-voice'
      version: ${{ inputs.version || '' }}
