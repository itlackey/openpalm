# Assessment — #433: Guardian state store (bun:sqlite): ingress/client registry + optional restart persistence

_Codebase assessment against 0.13.0-beta.3 (2026-07-12). Effort: **S**._

## Current state (already merged)

Phase 1 is essentially SHIPPED (landed as 0.12.0 workstream WS-D D1/D2b, see .github/roadmap/0.12.0/implementation-plan.md:217-236). Concretely: (1) /home/user/openpalm/packages/guardian/src/state-db.ts — bun:sqlite `principals` table (id, kind CHECK('portal','direct'), label, token_hash, enabled, created_at), SHA-256 token hashing, dir mkdir 0700 + chmod 0600 on the DB file, typed accessors (list/get/upsert/rotate/setEnabled), boot seeding from PORTAL_*_SECRET_FILE env (seedPortalPrincipalsFromEnv), and an idempotent 'channel'→'portal' kind-constraint migration via sqlite_master sniffing. DB path: GUARDIAN_STATE_DB_PATH ?? $HOME/state.db; in the stack HOME=/opt/openpalm/guardian which is bind-mounted from ${OP_HOME}/data/guardian (packages/skeleton/system/stack/portals.compose.yml:158), so the file lands at data/guardian/state.db as the issue requires. (2) /home/user/openpalm/packages/guardian/src/auth.ts — the registry IS the auth layer: Basic-auth parsing, constant-time hash compare, bounded in-memory principal cache (PRINCIPAL_CACHE_MAX=1024, FIFO eviction, negative caching) with invalidatePrincipalCache() called on every admin mutation — satisfies the 'cached, no per-request disk hit' invariant. Pluggable AuthStrategy seam (setAuthStrategy) exists for #435. (3) /home/user/openpalm/packages/guardian/src/admin.ts — admin CRUD API on the loopback-only :3831 listener: GET/POST /admin/principals, POST /admin/principals/:id/rotate|enable|disable, bearer-token authed via GUARDIAN_ADMIN_TOKEN_FILE (token hashes never returned). (4) MCP protocol token is registry-integrated: mcp.ts seedMcpPrincipalFromToken() upserts a 'direct' principal. (5) Nonce/replay store no longer exists at all (deleted with the HMAC transport in 0.12.0 D4 PR-3); rate-limit.ts remains purely in-memory (FixedWindowLimiter) — the 'explicitly NOT migrated' criterion is satisfied. (6) Tests exist and pass: state-db.test.ts (migration + idempotency), auth.test.ts (Basic happy/sad, cache, cap), admin.test.ts, plus server/proxy/cors/mcp tests all drive real spawned guardians against temp GUARDIAN_STATE_DB_PATH files. NOT implemented vs the issue text: WAL mode (no PRAGMA journal_mode anywhere in state-db.ts), PRAGMA user_version migration scheme (bespoke sqlite_master check instead), the protocols/persona/rate_policy columns (deliberately trimmed in the 0.12.0 plan as zero-consumer — 'added by ALTER TABLE ADD COLUMN when their consumer ships'), a hard-DELETE admin endpoint (disable only), any admin-UI tab for principals (explicitly deferred; zero hits for /admin/principals in packages/ui/src), and all of optional Phase 2 (ownership.ts is still an in-memory BoundedTtlMap; the A2A task store was never built — A2A deferred entirely in D3).

## Remaining work

- [ ] Enable WAL mode in openDatabase() (packages/guardian/src/state-db.ts:70-92): add db.exec('PRAGMA journal_mode = WAL') right after new Database(...); the issue's design section names WAL explicitly and it is a 2-line change + 1 assertion.
- [ ] Introduce PRAGMA user_version-based migration bookkeeping in state-db.ts, replacing/wrapping the sqlite_master 'channel'-string sniff (migrateKindConstraintIfNeeded, state-db.ts:32-68), so the future column adds promised to #435 (protocols/persona/rate_policy, users/roles tables) have a clean versioned path. Keep the existing migration as version 0→1.
- [ ] Decide and record the disposition of the unshipped registry columns (protocols, persona binding, rate_policy): the 0.12.0 plan (implementation-plan.md:219) deliberately deferred them as zero-consumer (rate limiting is env-constant in rate-limit.ts:17-27; MCP is a seeded principal; persona binding #22 has no consumer). Either close #433 documenting ALTER-TABLE-on-consumer as the policy, or add the columns now if #435's authn work in this milestone consumes them — coordinate with #435 before closing.
- [ ] Optionally add DELETE /admin/principals/:id to packages/guardian/src/admin.ts (current CRUD is list/create/rotate/enable/disable — no row removal), with invalidatePrincipalCache(id).
- [ ] Decide Phase 2 (explicitly optional in the issue): persist ownership.ts (packages/guardian/src/ownership.ts — BoundedTtlMap, 15m TTL) into state-db only if the orphan-on-restart window demonstrably bites; the A2A task store does not exist (A2A deferred), so that half is moot. If declining, note it in the issue closure — the issue text itself gates Phase 2 on observed need.
- [ ] Update issue checkboxes / close-out note: nonce-store criterion is satisfied vacuously (replay.ts deleted with HMAC in 0.12.0 D4 PR-3, so nothing was migrated); confirm 'admin UI manages rows' was consciously narrowed to admin API + curl per implementation-plan.md:236 (no UI tab until direct ingress has users — coordinate with #556 if a UI tab is still wanted, but #556 is not in this milestone's open set).

## Affected files

- `/home/user/openpalm/packages/guardian/src/state-db.ts`
- `/home/user/openpalm/packages/guardian/src/state-db.test.ts`
- `/home/user/openpalm/packages/guardian/src/auth.ts`
- `/home/user/openpalm/packages/guardian/src/auth.test.ts`
- `/home/user/openpalm/packages/guardian/src/admin.ts`
- `/home/user/openpalm/packages/guardian/src/admin.test.ts`
- `/home/user/openpalm/packages/guardian/src/ownership.ts`
- `/home/user/openpalm/packages/guardian/src/rate-limit.ts`
- `/home/user/openpalm/packages/guardian/src/mcp.ts`
- `/home/user/openpalm/packages/guardian/src/server.ts`
- `/home/user/openpalm/packages/skeleton/system/stack/portals.compose.yml`
- `/home/user/openpalm/docs/technical/core-principles.md`

## Test strategy

Guardian tests run with `cd packages/guardian && bun test --no-orphans` (package.json script "test"; verified green — the state-db/auth/admin/proxy-direct subset passes with 11 tests / 1549 expects). Test-first pattern to follow: pure store logic goes in state-db.test.ts using a per-test mkdtempSync temp dir; anything touching openDatabase()'s module singleton is driven via a spawned guardian process with GUARDIAN_STATE_DB_PATH pointed at a temp file (see auth.test.ts:22, proxy-direct.test.ts:156, cors.test.ts:143 for the fixture pattern). New assertions: (1) WAL — open the DB after boot and assert `PRAGMA journal_mode` returns 'wal' and file mode is 0600 (statSync().mode & 0o777); (2) user_version — assert version bumps once and re-boot is a no-op on an already-migrated DB (mirror the existing idempotency test at state-db.test.ts:104); (3) DELETE endpoint — extend admin flow tests (proxy-direct.test.ts registers principals via POST /admin/principals) with delete + subsequent 401 on the deleted principal, proving cache invalidation; (4) if Phase 2 ownership persistence is pursued, add a restart test: spawn guardian, create session ownership, kill, respawn against same GUARDIAN_STATE_DB_PATH, assert the session is still authorized.

## Dependencies (0.13.0 open issues)

- #435 (guardian authn) — soft, reverse-direction: #435 builds on #433's principals table and AuthStrategy seam (auth.ts setAuthStrategy). The only forward coupling is the close-out decision on the deferred protocols/persona/rate_policy columns and any users/identities schema, which should be settled with #435 so the schema is extended once (the issue's own 'Forward-looking (#435)' note; the generic Principal row shape it asked for is already in place).
- No other open 0.13.0 issue (486, 488, 490, 491, 506, 511, 557, 563) blocks or is blocked by the remaining #433 work; #557 (guardian TLS/CORS) and #511 (PWA) merely consume the same guardian auth surface.

## Risks

- Enabling WAL creates -wal/-shm sidecar files next to state.db inside the bind-mounted ${OP_HOME}/data/guardian dir; they are created with default umask, so the 0600 chmod discipline must be extended to them (or PRAGMA locking semantics documented) to keep the 'guardian-private' invariant.
- Retrofitting PRAGMA user_version onto DBs that already ran the sqlite_master-based migration must treat both 'old CHECK constraint' and 'new constraint but user_version=0' states correctly, or upgraders from 0.12.x could re-run/skip migrations.
- Adding registry columns now (protocols/persona/rate_policy) without consumers contradicts the repo's zero-dead-code policy that got them trimmed in 0.12.0 — doing it speculatively risks review pushback; doing it late risks #435 reshaping the schema anyway. The decision needs an explicit owner call.
- Phase 2 ownership persistence changes hot-path write behavior (every session create/permission record becomes a DB write); the issue's own principle warns against disk I/O on hot paths — if pursued, writes must be batched/async and the in-memory map kept authoritative.
- Module-level singleton `db` in state-db.ts plus GUARDIAN_STATE_DB_PATH read at import time means in-process tests share one DB per process; new tests must keep using spawned-process or temp-dir patterns to avoid cross-test bleed (already noted in state-db.test.ts:11-14).

---

## Original issue body

# #433: Guardian state store (bun:sqlite): ingress/client registry + optional restart persistence

# Guardian state store (`bun:sqlite`): ingress/client registry + optional restart persistence

As the guardian grows from "HMAC channels only" to **multiple ingress surfaces** (HMAC channels + OpenCode-proxy #429 + MCP + A2A #432), its per-ingress configuration and runtime state outgrow env vars + in-memory Maps. Add a small **guardian-local SQLite DB** (`bun:sqlite` — **built into Bun, no new dependency**) for the things that genuinely benefit from a table or from surviving a restart. Keep ephemeral hot-path state in memory.

## Motivation
- **Growing ingress surface → growing config.** Each ingress needs a token, an enabled flag, a bind policy, a rate policy, and (with #22) a persona binding. Today these are env vars (`CHANNEL_*_SECRET_FILE`, `OP_GUARDIAN_*_TOKEN`, …). That surface balloons as ingress options multiply — the opposite of #395's env-reduction goal. A small registry table scales better and is queryable by the admin UI.
- **Restart resilience.** Ownership maps and the A2A task store are in-memory; a guardian restart orphans sessions (today mitigated by broadcasting a synthetic `session.error`). A durable store removes the orphan window.

## What goes in SQLite (and what does NOT)
| State | Today | Move to SQLite? | Why |
|---|---|---|---|
| **Ingress/client registry** (per channel/client: token hash, enabled protocols, persona binding, rate policy) | env vars | ✅ **yes** | Scales the growing config surface; queryable; complements #395 |
| **Session/permission ownership** (`ownership.ts`) | in-mem Map, 50k cap, 15m TTL | ⚖️ **optional** | Survives restart → no orphaned sessions; low write volume |
| **A2A task store** (#432) | `InMemoryTaskStore` | ⚖️ **optional** | Durable `tasks/get` across restart; v1 can stay in-memory |
| **Audit log** (`audit.ts`) | JSONL file | ⚖️ **optional** | Queryable/rotatable; file is fine for v1 |
| **Nonce/replay store** (`replay.ts`) | in-mem Map, 50k cap, 5-min window | ❌ **no** | Ephemeral *by design*; a 5-min window needs no persistence; per-request disk I/O for zero security gain |
| **Rate-limit buckets** (`rate-limit.ts`) | in-mem Map, 10k cap | ❌ **no** | Ephemeral counters; same reasoning |

**Principle:** persist what benefits from durability or scales as a table (registry, ownership, tasks, audit). Keep short-window ephemeral state (nonce, rate) in memory — moving it to disk adds hot-path I/O without benefit.

## Design
- **`bun:sqlite`** (`import { Database } from "bun:sqlite"`), WAL mode, one DB file under the guardian's data dir (`data/guardian/state.db`), `0600`. **No new dependency** (Bun built-in — satisfies the no-dep-duplicates-a-Bun-built-in rule).
- A thin `core/guardian/src/store.ts` with typed accessors (no ORM, no abstraction layer — per the zero-abstraction policy). Schema migrations are a simple `PRAGMA user_version` step on boot.
- **Phase 1 — ingress/client registry only.** One `ingress` table (`id, kind, token_hash, enabled, protocols, persona, rate_policy, created_at`). The admin UI manages rows; the guardian reads them at request time (cached, short TTL). This alone delivers the "growing ingress list" value and trims env vars.
  - **This table IS the guardian's custom auth layer (#429).** Each row is a unique **`channelId:channelSecret`** (the Basic-auth credential a channel/client presents). The guardian parses the inbound `Authorization` header itself (it's our Bun server, not bound to OpenCode's single-password model), validates against `token_hash`, and resolves the row → a `Principal` with its rules (protocols, persona, rate). The **Basic creds are the identity** — per-channel principal over standard Basic/Bearer transport. Replaces `CHANNEL_*_SECRET_FILE`. (End-user-within-a-channel identity is a *separate, optional* metadata concern — see #429/#434 — not a registry credential.)
- **Phase 2 (optional) — ownership + A2A task persistence.** Back `ownership.ts` and the A2A task store with SQLite tables, keeping the same TTL/eviction discipline (now durable). Gated on whether the orphan-on-restart window actually bites in practice.
- **Never** SQLite-back the nonce or rate-limit stores.

## Security invariants
- DB is **guardian-private** (`data/guardian/`, `0600`), never mounted elsewhere; tokens stored **hashed** (never plaintext), consistent with the secret-file model.
- No change to ingress posture: registry rows only *describe* ingress; they don't grant network exposure (still loopback-default + `OP_BIND_ADDRESS` opt-in).
- Reads on the request hot path must be cached (short TTL) so the registry lookup isn't a per-request disk hit.

## Acceptance criteria
- [ ] `bun:sqlite`-backed `core/guardian/src/store.ts`; no new npm dependency added to the guardian.
- [ ] Phase 1: an `ingress` registry table + admin CRUD; per-ingress token (hashed), enabled flag, protocols, persona, rate policy read from the DB (cached) instead of new env vars.
- [ ] Nonce + rate-limit stores remain in-memory (explicitly NOT migrated).
- [ ] DB file is `data/guardian/state.db`, `0600`, guardian-private; tokens hashed at rest.
- [ ] `cd core/guardian && bun test` passes (store accessors + cache + hot-path-no-disk-on-nonce covered).

## Relationships
- Enables/cleans up **#432** (Guardian Gateway) — the registry holds the per-protocol tokens (`mcp`/`a2a`/`direct`) and the A2A task store can later be SQLite-backed.
- Complements **#395** (env-surface reduction) — moves the growing per-ingress token/config out of env vars.
- Carries the **#22** persona→channel binding as a registry column rather than a separate file/env.
- **Forward-looking (#435):** model the row as a generic **`Principal`** (machine *or* human), not "channel" specifically, so the future true auth/authz layer (OAuth/OIDC + multi-user) can add a users/identities + roles/policy table here (or federate to an external IdP) without reshaping the schema.

---

<sub>🤝 Spun out of the gateway/ingress evaluation (2026-06-05): `bun:sqlite` is zero-dep; persist the registry (+ optionally ownership/tasks), keep ephemeral nonce/rate in memory. Effort: S (Phase 1) / M (with Phase 2 persistence).</sub>
