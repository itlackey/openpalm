/**
 * Connection migration flags — re-exported from @openpalm/lib.
 */
export type { ConnectionMigrationFlags, ConnectionCompatibilityMode } from "@openpalm/lib";

export {
  readConnectionMigrationFlags,
  detectConnectionCompatibilityMode,
} from "@openpalm/lib";
