/**
 * Home layout path resolution — re-exported from @openpalm/lib.
 */
export {
  resolveOpenPalmHome,
  resolveConfigDir,
  resolveVaultDir,
  resolveDataDir,
  resolveLogsDir,
  resolveCacheHome,
  resolveRegistryCacheDir,
  ensureHomeDirs,
} from "@openpalm/lib";
