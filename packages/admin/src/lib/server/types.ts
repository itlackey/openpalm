/**
 * Shared types and constants — re-exported from @openpalm/lib.
 */
export type {
  CoreServiceName,
  OptionalServiceName,
  AccessScope,
  CallerType,
  ChannelInfo,
  AuditEntry,
  ArtifactMeta,
  ControlPlaneState,
} from "@openpalm/lib";

export {
  CORE_SERVICES,
  OPTIONAL_SERVICES,
} from "@openpalm/lib";
