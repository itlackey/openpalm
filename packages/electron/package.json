{
	"name": "@openpalm/electron",
	"version": "0.13.6",
	"private": true,
	"type": "module",
	"description": "Optional static desktop administration tool for OpenPalm stacks",
	"license": "MPL-2.0",
	"main": "dist/admin-main.js",
	"scripts": {
		"start": "electron .",
		"typecheck": "tsc --noEmit",
		"bundle": "bun build src/admin-main.ts --bundle --target=node --outfile dist/admin-main.js --external electron && bun build src/admin-preload.ts --bundle --target=node --format=cjs --outfile dist/admin-preload.cjs --external electron",
		"build:mac": "bun run bundle && electron-builder --config electron-builder.lean.yml --mac",
		"build:linux": "bun run bundle && electron-builder --config electron-builder.lean.yml --linux",
		"build:linux-x64": "bun run bundle && electron-builder --config electron-builder.lean.yml --linux --x64",
		"build:win": "bun run bundle && electron-builder --config electron-builder.lean.yml --win"
	},
	"devDependencies": {
		"@openpalm/lib": "workspace:*",
		"electron": "43.2.0",
		"electron-builder": "^26.15.3",
		"typescript": "^6.0.3",
		"@types/node": "^26.1.2"
	}
}
