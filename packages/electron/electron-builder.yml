appId: com.openpalm.app
productName: OpenPalm
copyright: "Copyright © 2025 OpenPalm Contributors"

directories:
  output: dist/packages
  buildResources: assets

files:
  - dist/main.js
  - dist/preload.cjs
  - assets/**/*
  - package.json

extraResources:
  - from: ../../packages/ui/build
    to: ui-build
    filter: "**/*"
  - from: admin-tools/dist/index.js
    to: admin-tools/index.js
  - from: ../../.openpalm
    to: openpalm-skeleton
    filter: "**/*"

mac:
  category: public.app-category.developer-tools
  target:
    - target: dmg
      arch: [arm64, x64]
  icon: assets/icon.png

linux:
  target:
    - target: AppImage
      arch: [x64, arm64]
  icon: assets/icon.png

win:
  target:
    # nsis = the "Setup.exe" installer (Start Menu, uninstaller, electron-updater
    # auto-update via the .blockmap). zip = a portable, no-install build users can
    # just extract and run.
    - target: nsis
      arch: [x64]
    - target: zip
      arch: [x64]
  icon: assets/icon.png

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true

npmRebuild: false
generateUpdatesFilesForAllChannels: true
publish:
  provider: github
  owner: itlackey
  repo: openpalm
  releaseType: release
