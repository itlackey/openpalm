# OpenPalm custom services and overlays.
#
# This file is intentionally empty by default. Operators can edit it directly
# to add custom containers or make deliberate local overrides. OpenPalm audits
# core trust boundaries before lifecycle commands activate this overlay.

services: {}
