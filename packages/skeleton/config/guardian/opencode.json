{
  "$schema": "https://opencode.ai/config.json",

  // Pin a SMALL, fast model from your provider for moderation — it runs on
  // suspicious messages and should be cheap. Credentials are shared via
  // knowledge/secrets/auth.json (same provider as the assistant). Edit freely.
  "model": "opencode/big-pickle"
}
