import defaultYaml from "./openpalm.yaml" with { type: "text" };
import defaultSecrets from "./secrets.env" with { type: "text" };

export const DEFAULT_OPENPALM_YAML = defaultYaml;
export const DEFAULT_SECRETS_ENV = defaultSecrets;
