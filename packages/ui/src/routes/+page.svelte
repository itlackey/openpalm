<!-- Root route: never renders — hooks.server.ts (document navigations) and
     +page.server.ts (client-side navigations) both redirect to the landing
     resolved by resolveLanding() (plan ui-runtime-modes-plan.md §6.5). -->
