{
  "name": "@openpalm/claude-desktop",
  "version": "0.14.0",
  "private": true,
  "license": "MPL-2.0",
  "type": "module",
  "description": "Optional Claude Desktop MCPB bridge for a local OpenPalm Guardian",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "pack": "bun run build && bun run scripts/pack.ts",
    "test": "bun run build && bun test src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "2.1.2",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
