# setup.env — Deployment config for OpenPalm Azure VM.
#
# Copy to setup.env and fill in your values.
# This file is sourced by bash. Quote values with special characters.
# Secrets go in secrets.env (not here).

# ── Required ──────────────────────────────────────────────────────────

AZURE_SUBSCRIPTION_ID=your-subscription-id
SETUP_SPEC_FILE=./my-setup.yaml

# ── Optional (defaults shown) ────────────────────────────────────────

# LOCATION=eastus
# RESOURCE_GROUP=rg-openpalm-vm
# ADMIN_USERNAME=openpalm
# OPENPALM_VERSION=v0.10.0
# STORAGE_NAME=stopenpalm
# BACKUP_SHARE=openpalm-backups
# KV_NAME=kv-openpalm
