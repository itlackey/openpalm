# core/admin

Container build assets for the admin service.

- `Dockerfile` builds the runtime image for the SvelteKit control plane app in `packages/admin/`.
- App source, tests, static assets, and workspace metadata live in `packages/admin/`.

For application development, see `packages/admin/README.md`.
