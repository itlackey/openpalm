---
name: memory-recall
description: Search memory for relevant stored facts
arguments:
  - name: query
    description: The search query
    required: true
---

Use the `memory-query` tool to search OpenMemory for facts matching "$ARGUMENTS".

Return the results in a clear, readable format. Include memory IDs if available.
