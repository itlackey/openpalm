---
name: memory-save
description: Save content to memory for later recall
arguments:
  - name: text
    description: The content to save
    required: true
---

Use the `memory-save` tool to persist the following content to OpenMemory:

$ARGUMENTS

Confirm what was saved and include the memory ID if returned.
